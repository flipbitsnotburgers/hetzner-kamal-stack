# GitHub Container Registry credentials
# Create a PAT at: https://github.com/settings/tokens/new
# Required scope: write:packages
KAMAL_REGISTRY_PASSWORD=ghp_your-github-token

# Database credentials
# Generate a secure password: openssl rand -base64 32
POSTGRES_PASSWORD=change-me-to-secure-password
DATABASE_URL=postgres://postgres:${POSTGRES_PASSWORD}@${HETZNER_PROJECT_NAME:-test}-accessories-1:5432/postgres?sslmode=disable
